{
  "name": "the-atelier-scripts",
  "version": "1.0.0",
  "description": "scripts (scraping, AI analysis) for the-atelier — separate from the static site itself.",
  "private": true,
  "scripts": {
    "scrape:postype": "node scripts/scrape-postype.js",
    "analyze:trends": "node scripts/analyze-trends.js",
    "trends:postype": "node scripts/scrape-postype.js | node scripts/analyze-trends.js"
  },
  "dependencies": {
    "puppeteer": "^23.0.0",
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "engines": {
    "node": ">=20"
  }
}
